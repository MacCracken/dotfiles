# Neon Jungle Theme - Top Bar Layout

## 🎨 Design Changes
* **Position**: The status bar (Waybar) is now pinned to the top.
* **Gaps**: Increased gaps to 20px to better highlight the nature/overgrowth wallpaper.
* **Borders**: Thicker 3px borders with a 45-degree neon gradient.

## 🛠 Implementation
1. Move the `hyprland.conf` to `~/.config/hypr/`.
2. Place the `style.css` in `~/.config/hypr/waybar/`.
3. In your Waybar `config.jsonc`, ensure `"position": "top"` is set.
